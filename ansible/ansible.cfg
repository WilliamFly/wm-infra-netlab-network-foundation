[defaults]
inventory      = inventory.ini
roles_path     = roles
host_key_checking = False
# host_key_checking=False is a lab convenience (VMs get rebuilt often, so
# their host key keeps changing) — leave this on in any real environment.
